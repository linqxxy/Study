package com.linqxxy.git.wwh1;

public interface IUtill1<P ,R> {
    /*
     *将参数类型为Integer的值转化为String
     * y=f(x)
     * x:Integer
     * y:String
     * f:逻辑转换
     */
     R convert(Integer value);
}
